import 'package:flutter/material.dart';

class RegisterDrink extends StatelessWidget {
  const RegisterDrink({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(
            child: Column(
      children: [Text("register page")],
    )));
  }
}
